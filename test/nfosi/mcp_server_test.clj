(ns nfosi.mcp-server-test
  (:require [clojure.test :refer [deftest is testing]]
            [nfosi.mcp-server :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
