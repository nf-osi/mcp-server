[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nfty"
version = "0.1.0"
description = "MCP server for NFty tools supporting NF Data Portal workflows"
readme = "nfty/README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "NF-OSI", email = "nf-osi@sagebionetworks.org"}
]
keywords = ["mcp", "synapse", "curation", "neurofibromatosis"]

dependencies = [
    # 2.x for MCPServer (FastMCP's 2.0 rename): mcp.add_tool derives each
    # tool's JSON Schema from its function signature and its description from
    # the docstring, replacing the hand-written Tool definitions and the
    # list_tools/call_tool dispatch this server used before.
    "mcp>=2.2,<3",
    "synapseclient>=4.0.0",
    "requests>=2.31.0",
    "jsonschema>=4.19.0",
    "python-json-logger>=2.0.0",
    "pandas>=1.5.0",
    "numpy>=1.23.0"
]

[project.urls]
Homepage = "https://github.com/nf-osi/mcp-server"
Repository = "https://github.com/nf-osi/mcp-server"
Issues = "https://github.com/nf-osi/mcp-server/issues"

[project.scripts]
nfty = "nfty.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["nfty"]
